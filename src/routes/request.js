const express = require("express");
const requestRouter = express.Router()

//api to send request : either interest / ignore
//api to review request : either accept / reject



module.exports = requestRouter;